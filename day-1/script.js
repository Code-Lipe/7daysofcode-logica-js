let numberOne = 1;
let stringOne = '1';
let numberThirty = 30;
let stringThirty = '30';
let numberTen = 10;
let stringTen = '10';


if(numberOne === stringOne){
    console.log('The variables numberOne and stringOne have the same value and the same type.');
}
else if (numberOne == stringOne) {
    console.log('The numberOne and stringOne variables have the same value but different types.');
} 
else {
    console.log('The numberOne and stringOne variables do not have the same value.');
};

if (numberThirty === stringThirty) {
    console.log('The variables numberThirty and stringThirty have the same value and the same type.');;
}
else if(numberThirty == stringThirty){
    console.log('The numberThirty and stringThirty variables have the same value but different types.');
}
else {
    console.log('The numberThirty and stringThirty variables do not have the same value.');
};

if (numberTen === stringTen) {
    console.log('The variables numberTen and strinrTen have the same value and the same type.');;
}
else if(numberTen == stringTen){
    console.log('The numberTen and strinrTen variables have the same value but different types.');
}
else {
    console.log('The numberTen and strinrTen variables do not have the same value.');
};
